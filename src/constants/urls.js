const baseURL = 'https://jsonplaceholder.typicode.com'

const users = '/users'

const comment = '/comments'

const urls = {
        base:users,
        comment:comment,

}

export {
    baseURL,
    urls
}